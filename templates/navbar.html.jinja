<header class="navbar">

    <ul class="navbar-list">
        <a href="/">Home</a>
        <a href="/about">About</a>
    </ul>

    <div class="navbar-login">
    {% if session.user %}
        <p>Logged in as {{session.user.name}}</p>
        {% if session.user and session.user.is_admin == True %}
            {# <a href="/add">Add New Surfboard</a> DELETE IF THE BUTTON IS WORKING FINE#}
            <button id="add-btn" onclick="addRoute()">Add New Surfboard</button>
        {% endif %}
        <form action="/logout" method="POST">
        <button>Logout</button>
        </form>
    {% else %}
        {# <a href="/login">Shaper Login</a> DELETE IF THE BUTTON IS WORKING FINE#}
        <button id="login-btn">Shaper Login</button>
    {% endif %}
    </div>

</header>








