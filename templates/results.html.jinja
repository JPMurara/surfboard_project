<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Surfboard Selector - Results</title>
    {% include "css-link.html.jinja" %}
</head>
<body>
  {% include "navbar.html.jinja" %}
    
  <section class="banner-intro">
    <h1>Some Magic Surfboards For You</h1>
  </section>

  <div class="results-container">

    <div class="results-row">
      <div class="results-column">Brand</div>
      <div class="results-column">Model</div>
      <div class="results-column">Board Type</div>
      <div class="results-column">Wave Range (ft)</div>
      <div class="results-column">Wave Type</div>
      <div class="results-column">Break Type</div>
      <div class="results-column">Skill Level</div>
      <div class="results-column"></div>
      <div class="results-column"></div>
      <div class="results-column"></div>
    </div>

    {% for surfboard in exact_match %}
    <div class="results-row grid-align">
      <div class="results-column" >{{surfboard.shaper_name}}</div>
      <div class="results-column" >{{surfboard.model_name}}</div>
      <div class="results-column" >{{surfboard.model_type}}</div>
      <div class="results-column" >
      {{ surfboard.min_wave_size }} to {{ surfboard.max_wave_size }}
      </div>
      <div class="results-column" >{{surfboard.wave_type}}</div>
      <div class="results-column" >{{surfboard.break_type}}</div>
      <div class="results-column" >{{surfboard.skill_level}}</div>
      <img  class="results-img" src="{{surfboard.img_single}}" alt="">
      <button class="results-column details-btn button" data-url="{{ surfboard.surfboard_url }}">More details</button>
      <button  class="results-column button btn-remove">Remove</button>
    </div>
    {% endfor %}
    </div>

{% include "footer.html.jinja" %}
</body>
</html>